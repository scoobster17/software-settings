################################################################################

### BASH LOCAL ###

# Changes require the following command to be run unless the terminal is closed:
# "source ~/.bashlocal" or "rb" using alias from .bashrc

# This file assumes a bash session is running this code; tested in iTerm2 (OSX)
# and Git Bash via ConEmu Portable (Windows 10)

echo -e "Reloading .bashlocal"

################################################################################

## LOCAL SETTINGS

PROJECTS_DIR="/path/to/dir" # Path to locally stored projects.
                            # To be changed when copied to local
echo "Projects directory set to \""$PROJECTS_DIR"\""
